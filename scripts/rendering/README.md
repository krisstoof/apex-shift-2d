# Rendering

This directory is intended for rendering-specific code.

## Purpose

- Keep world, terrain, minimap, and HUD presentation logic together.
- Isolate visual concerns from portable gameplay logic.
- Keep rendering code separate from core gameplay rules and runtime orchestration.

This directory documents the rendering layer only. Existing behavior should remain unchanged.
